# Project-wide Gradle settings.
#
# Machine-specific settings (like which JDK to build with) don't belong here --
# put those in your user-level Gradle properties file instead
# (%USERPROFILE%\.gradle\gradle.properties on Windows), so they never get
# committed and never affect anyone else's build.
